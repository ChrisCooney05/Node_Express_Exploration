extends layout

block content
  h1= title 
  p Welcome to #[em LocalLibrary], a very basic Express website developed as a learning exercise.
  //using h1= makes title a variable so when the data is passed in it will change accordingly
  h1 Dynamic Content 
  
  if error 
    p Error getting dynamic content 
  else 
    p The library has the following record counts: 

    ul 
      li #[strong Books:] !{data.bookCount}
      li #[strong Copies:] !{data.bookInstanceCount}
      li #[strong Copies available] !{data.bookInstanceAvailableCount}
      li #[strong Authors:] !{data.authorCount}
      li #[strong Genres:] !{data.genreCount}
